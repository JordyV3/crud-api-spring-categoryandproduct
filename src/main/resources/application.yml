spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/crud-app
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
